# Keep secrets, development assets, and unrelated services out of the API image.
.env
.env.*
.git
.gitignore
.claude/
.venv
venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
web/
deploy/
docs/
tests/
node_modules/
.next/
AGENTS.md
README.md
compose.yaml
.DS_Store

# Deployment manifests. They describe how the image is run, and are read by the
# CI host rather than by anything inside it.
.drone.yml
jobspec.nomad
jobspec_acc.nomad
