# Complete backend runtime deps for the FastAPI gateway + all services.
# Used for deployment (e.g. Render).
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0
pydantic>=2.6
eval-type-backport>=0.2; python_version < "3.10"

# LLM + config/vocab
openai>=1.0.0
anthropic>=0.49.0
pyyaml>=6.0

# Document parsing (chunker)
pdfplumber>=0.11.0
python-docx>=0.8.11
python-pptx>=1.0.0
pypdfium2>=4.30.0
pillow>=10.0.0

# PubMed/PMC backend uses stdlib urllib — no extra dep.
