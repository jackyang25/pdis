*
!web/
!web/**
# `*` excludes top-level entries only, so these rules cannot gate inside shared/.
# The Dockerfile copies shared artifacts individually for that reason.
!shared/
!shared/product_knowledge.json
!shared/prompt_reference.json

web/node_modules/
web/.next/
web/.env*.local
web/npm-debug.log*
web/.DS_Store
